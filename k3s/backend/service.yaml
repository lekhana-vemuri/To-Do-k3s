apiVersion: v1
kind: Service

metadata:
  name: todo-backend-service
  namespace: todo-3tier

spec:
  selector:
    app: todo-backend

  ports:
  - name: backend-port
    protocol: TCP
    port: 3001
    targetPort: 3001

  type: LoadBalancer