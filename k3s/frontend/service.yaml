apiVersion: v1
kind: Service

metadata:
  name: todo-frontend-service
  namespace: todo-3tier

spec:
  selector:
    app: todo-frontend

  ports:
  - port: 3001
    targetPort: 80

  type: LoadBalancer