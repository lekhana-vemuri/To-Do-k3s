apiVersion: v1
kind: Service
metadata:
  name: todo-frontend-service
spec:
  selector:
    app: todo-frontend
  ports:
  - name: frontend-port
    protocol: TCP
    port: 3000
    targetPort: 80
  type: LoadBalancer
